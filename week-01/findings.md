
# Week 1 Findings

- Most outbound traffic uses TCP 443 (HTTPS)
- DNS traffic occurs continuously even at idle
- Several background services initiate outbound connections automatically
- Port scanning creates a clear pattern of sequential SYN packets
